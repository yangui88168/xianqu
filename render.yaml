services:
  - type: web
    name: xianqu-server
    env: node
    buildCommand: "pnpm install && pnpm run build"
    startCommand: "cd server && node dist/index.js"
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
